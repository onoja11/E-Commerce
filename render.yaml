services:
  - type: web
    name: my-vite-react-app   # change this to your app name
    env: static
    buildCommand: npm run build
    staticPublishPath: dist

    routes:
      - type: rewrite
        source: /*
        destination: /index.html
